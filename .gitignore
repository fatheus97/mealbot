# Prerequisites
.env
.env.local
# Local host-dev env overrides (e.g. .env.local.hostdev — Vite proxy target)
.env.local.*
.env.production

# Python (Backend)
**/__pycache__/
**/*.py[cod]
**/.venv/
backend/mealbot.db
*.db

# Node (Frontend)
**/node_modules/
**/dist/
frontend/.next/

# IDEs
**/.idea/
**/.vscode/
**/.DS_Store

# Claude Code local settings (user-specific permissions)
.claude/settings.local.json
# Claude Code runtime lock (ScheduleWakeup / loop state, machine-local)
.claude/scheduled_tasks.lock
# Claude Code browser-preview dev-server config (per-developer, regenerated)
.claude/launch.json
